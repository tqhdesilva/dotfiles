# pi package

This stow package syncs global Pi agent configuration and shared agent instructions.

Managed targets:

- `~/AGENTS.md`
- `~/.pi/agent/settings.json`

`install.sh` backs up existing unmanaged copies before stowing this package.
